---
description: レビュー待ちを優先順位順に人力テストをステップ・ナビ → 承認で本番待ちへ
argument-hint: "[empty|<page>|--list]"
disable-model-invocation: true
model: sonnet
---

# /review — レビュー待ちを優先順位順に人力テストをステップ・ナビ → 承認で本番待ちへ

> **【正本（~/cc-pipeline）・プロジェクト中立版】** 固有値をハードコードしない正本。**対象アプリ構成（apps/web 単体／web+mobile／web+LIFF 等）・画面パス・ローカルポート・本番ref・外部送信媒体（LINE/メール/push 等）は各リポの CLAUDE.md「Pipeline設定」の `APP_LAYOUT` / `APP_LAYOUT_NOTES` を参照**して具体化する。`{{DEV_URL}}` / `{{DEPLOY_PLATFORM}}` 等の `{{...}}` は CLAUDE.md「Pipeline設定」/`.env` 由来。UI/ロジックは原則ブラウザ（{{DEV_URL}}）で確認し、ネイティブ/LINEアプリ内など**実機依存は `⚠実機確認` として記録**する。

`/run` がレビュー待ちに積んだタスクを、**人間が１件ずつ手で確認するための対話ナビ**。各タスク本文の「🧪人力チェック手順」を**1ステップずつ提示**し、**毎ステップにテスト用URL・テナント/アカウント・期待結果を明示**して、人間の「OK/NG」を受けて進める。全ステップOKなら**レビュー承認＝ステータスをレビュー待ち→本番待ちに反映**する（CLAUDE.mdの唯一の人間ゲート「レビュー待ち→本番待ち昇格」を、この対話を通じて人間が実行する）。

> **大前提**: レビュー待ちのコードは **dev にマージ済みだが未push＝本番には出ていない**（{{DEPLOY_PLATFORM}}・dev はデプロイ対象外）。だから人力テストは原則 **ローカルで dev ブランチを動かして**行う。本番URLでの確認は ship 後（/ship）。本コマンドは「本番に出す前の最終人間チェック」。
>
> **緊急も dev にいる**（2026-08-09・T26）。2026-08-06〜08-09 の間だけ
> 「緊急は main派生 `hotfix/<slug>` を見る」という例外があったが、
> **緊急レーンそのものを廃止した**ので例外は消えた。全チケットが dev。
>
> 経緯: 緊急を独立ブランチにしていた理由（dev堆積を巻き込まず独立ship）は
> フラグ方式で解け、逆に「統合して初めて出る不具合が ship 直前まで隠れる」
> 「機能単位で通しテストできない」「**緊急ほど古い道具でレビューすることになる**」
> というコストが実測された。

引数 `$ARGUMENTS`:
- 空 … 自案件のレビュー待ちを**優先順位順**（緊急>高>中>低、同率は作成日古い順）に上から順にナビ。
- NotionページURL/ID/タスク名 … その1件だけをナビ。
- `--list` … レビュー待ち一覧（優先度順・リスク・エピック）を出すだけで終了（ナビしない）。
- `--staging` / 人が「**ステージングで**」と言った時 … 確認先をローカルではなく**ステージング**にする（§0-S）。他は全部同じ。

---

## §0-S ステージングでレビューする時（「ステージングで」と言われたら）

**何のためにあるか**: 人がPCの前に居ない時（外出中・移動中）に**スマホでレビューできる**ようにするため。
機械のゲート（build / E2E / typecheck）は**従来どおりローカルで回す**。差し替えるのは「人が見る場所」だけ。

```bash
node --env-file=.env scripts/seed-staging-demo.mjs   # レビューできるデータを作る（確認用* 接頭辞）
node scripts/deploy-staging.mjs                      # いまの作業ツリーを staging へ出す＋接続先を検証
```
- `deploy-staging.mjs` は **本番リンクの退避→差し替え→復元→復元されたことのassert** まで自動でやる。
  手で `apps/*/.vercel/project.json` を書き換えない（戻し忘れると次の本番デプロイが事故る）。
- **検証に失敗したらURLを人に渡さない**。スクリプトが exit 1 する。間違った環境をレビューさせるのが最悪。
- 片付けは `node --env-file=.env scripts/seed-staging-demo.mjs --clean`。

**★ステージングは本番Supabaseを見る（分離DBではない）。** テナントは `demo` に固定してある。
人に案内する時は毎回**「本番テナントのアカウントではログインしない」**を添える。
ログイン: `staging@email.com` / `sido-stg-2026!`（CLAUDE.md「STAGING」参照）

**ステージングでは確認しきれないもの**（正直に伝えてローカルに回す）:
- ローカルにしか無いデータに依存する確認（demoテナントには本番の実データ量が無い）
- 未pushの migration が要る機能（ステージングのDBは本番なので、本番に無い列は無い）
- LINEアプリ内固有（Flex体裁・友だち追加）は従来どおり `⚠実機確認`

**スマホで見ている前提の出し方**（§2 のステップ提示に上乗せ）:
- 1ステップに**操作を1つ**。長い前提説明を先に置かない（スマホでスクロールが辛い）。
- URLは必ずクリック可能な Markdown リンク。**深いパスまで直接飛ばす**（メニューを辿らせない）。
- 「横スクロールが要る表」「文字が小さい」等が出たら、それ自体を🤝の所見として拾う
  （PCでは気づけない指摘なので、ステージングでレビューする一番の収穫になりうる）。

## §0 プリフライト（1回）

1. **対象取得**: `node --env-file=.env scripts/next-target.mjs --board` を実行し、**「レビュー待ち」グループ**を読む（既に優先度順＋作成古い順にソート済み・各行は `[優先順位/リスク🧱土台]` 形式）。`$ARGUMENTS` 指定時は notion-fetch で直接取得。0件なら「レビュー待ちなし」を報告して終了。
2. **ローカル環境を起動**（dev コードをローカルで触るため・具体値は CLAUDE.md「Pipeline設定」LOCAL_STACK / APP_LAYOUT_NOTES）:
   - Supabase ローカル: 落ちていれば `supabase start`（ポートは LOCAL_STACK の設定に従う）。
   - **web（主）**: {{DEV_URL}}（未起動なら `pnpm dev` 等＝APP_LAYOUT の起動コマンド）。管理画面・顧客導線とも**原則ここ＝ブラウザで確認**する。
   - **local接続assert**（本番を誤って触らない）: 配信される Supabase URL が **local（LOCAL_STACK のポート）**を指し、**本番 ref（CLAUDE.md「Pipeline設定」記載の本番Supabase ref）ではない**ことを確認する。本番 ref を検出したら停止し `.env.local` で local 起動し直す。
     - **🔒 `curl -s {{DEV_URL}}/ | grep` は使わない**（2026-08-06 是正）。Vite/Nuxt の dev は
       `/` にシェルHTMLしか返さず、env はモジュール変換時に入るため、**本番を向いていても
       grep は何も返さない**。「何も出ない＝安全」と読めてしまう空振りの安全装置だった。
     - **エントリモジュールを叩く**: `grep -rl VITE_SUPABASE_URL <app>/src | head -1` で
       env を参照するファイルを見つけ、そのパスを dev サーバから取得して grep する
       （例: `curl -s {{DEV_URL}}/src/main.ts | grep -oE '<localポート>|<本番ref>'`）。
     - **🔒 fail-closed**: grep が **localポートも本番refも返さなかったら「安全」と判断しない**。
       「接続先を確認できなかった」として**停止する**（第22条: 0件と測れないを混同しない）。
       実例: sido の `apps/admin/.env` は本番 ref を持ち、`.env.local` が local で上書きしている。
       `.env.local` が消えれば本番を向くが、旧 assert はどちらの場合も無出力だった。
   - **ブランチ確認**: レビュー対象のコードが今のローカルに居るか。基本 `dev` を見る（`git branch --show-current` が dev か、無ければ `git checkout dev`）。作業中の差分があれば退避を促す。
     - **全チケットが dev にいる**（2026-08-09・T26 で緊急レーン廃止）。
       もし `hotfix/*` にしか無いものが出てきたら、それは**廃止前の残骸**なので
       dev にマージしてからレビューする（散らしたままレビューしない）。
   - **サーバー起動直後の初回アクセスは失敗しうる**（Vite/Next のコールドスタートでオンデマンドコンパイルが間に合わず、最初の1回だけ失敗/タイムアウトすることがある）。プロセス起動ログが出ただけで即人間にURLを渡さない。**渡す前に自分で該当URLを1回 `curl`（または軽いページ取得）して200が返ることを確認**してから案内する。人間が「開けない/ログインできない」と言ったら、まず自分で再現を試みてから聞き返す（同じ質問を繰り返させない）。
   - **テストアカウントを新規作成する場合、機能ゲート（課金/契約状態・権限フラグ等でブロックされる機能がある場合）を必ず一緒に解除する**：サインアップ直後のアカウントはプロジェクトによっては「未契約」「未承認」等で主要機能がブロックされる（具体的なゲート条件・必要なテーブル/フラグはCLAUDE.md「Pipeline設定」またはRLS構成メモ参照。例: `subscriptions`テーブルに`status=trialing`相当の行が無いと機能ゲートで弾かれる、等）。**アカウント作成と同じ手順の中で、ゲートを満たす最小限のシード（トライアル契約行の挿入等）まで一緒に行う**。後から「機能が使えない」と言われて都度追加するのではなく、最初から用意する。
3. **一覧プレビュー**: レビュー待ちを `① [高/🔴] タスク名（エピック）` の形で番号付き提示。`--list` ならここで終了。

---

## §1 各タスクのナビ開始（上から1件ずつ）

対象ページを notion-fetch し、本文の **「📋レビューダイジェスト」**と**「🧪人力チェック手順」**を読む。冒頭に**カード**を出す:

```
━━━━━━━━━ レビュー ① / N ━━━━━━━━━
🧪 [リスク] タスク名  （エピック）
変更概要: （📋ダイジェストの1〜2行）
本番反映に要るもの: （📋の env前提 / migration / cron・deploy）
⚠️ 要注意: （📋の Gemini二重レビュー指摘・⚠️要注意 があれば必ずここに赤字で。無ければ「なし」）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔗 テスト先（dev未push＝ローカルで確認）
  - web   : {{DEV_URL}}<該当パス>
  - 実機  : ⚠ ネイティブ/LINEアプリ内など実機固有（APP_LAYOUT_NOTES の該当導線）は実機確認。ブラウザで足りる UI/ロジックは {{DEV_URL}} で確認
  - DB    : ローカル Supabase（LOCAL_STACK のポート / テスト テナント・アカウント）
  - 使うデータ/前提: （🧪手順の「テストデータ・前提」をそのまま）
```

- **URLは必ずフルで具体化**する。🧪手順が画面名やパス（※実パスはプロジェクト固有＝CLAUDE.md「Pipeline設定」APP_LAYOUT_NOTES の画面パス例に合わせる）を指していれば、`{{DEV_URL}}` を前置した**クリックできる完全URL**にして出す。可変部（各種ID）はローカルにテスト用データを用意（または既存IDを案内）して**実URLを埋める**。
- **テスト先URLは必ず Markdown のクリック可能リンク `[URL](URL)` 形式で出力する**（生のテキストURLにしない＝ターミナルでそのまま開けるように）。カードの「🔗 テスト先」も §2 ステップの「📍 場所」も同様。Notion等の参照リンクも `[ラベル](URL)` で出す。
- 🔴高・🧱土台で ⚠️要注意（Gemini指摘等）がある場合は、**「この指摘を承知でレビューしますか？」を最初に確認**してから手順に入る。

### リスク別の検証手数（人の手数をリスクで変える）
- **🟢低（純・要人力確認カテゴリ非該当）**：本番待ち直行のためここに来ない（ノールック）。
- **🟢低でも「要人力確認カテゴリ該当」でここに来た場合（2026-07-02〜）**：/run が **金額/集計/データ統合・マージ/migration/認証・RLS/外部送信** に触れると判定してレビュー待ちへ回した🟢低。リスクは低くても**ミスの影響が大きい**ので、要点ステップ＋**「統合/集計/金額の結果が正しいか（統合漏れ・二重計上・孤児が無いか）を目視」**を必ず入れる（例：現場マージ後に現場別集計が統合先へ合算されているか）。
  - **★影響範囲マップの全参照を確認する（breadth）**：📋 の「依存/影響範囲」に /run が洗い出した参照（他テーブル・JSON内ID・各画面・集計/PDF/エクスポート・EF/バッチ）が並んでいる。**主画面だけでなく、その各参照が変更後も正しいかを1つずつ確認**する。今回の現場マージ事故は「マージ操作成功」だけ見て**現場別集計（下流の参照）を見落とした**＝影響範囲の広さを見る観点が主眼。📋に影響範囲マップが無い/薄いと感じたらそれ自体を指摘（=範囲判断不足）。
- **🟡中**：要点ステップのみ。ただし**「保存→再読込で永続するか」は必ず1ステップ入れる**（UI追加だけの見落とし＝DB制約/保存漏れを人の目でも拾う）。
- **🔴高/🧱土台**：詳細ステップ＋**CCが事前にデモデータを用意して"実URLで差が見える"状態にしてから提示**（人にデータ作成させない）。金額/集計系は「変更前後で数値が分かれる」を必ず目視させる。Gemini指摘は最初に⚠提示し承知確認。
- **デモデータの罠**：CCがデモを用意する時は ①insert失敗を verify する ②**対象テーブルの一意制約・upsert/on conflict 衝突に注意**（複合一意・PK 構成はプロジェクトのデータモデル参照）。**RLS スコープ（テナント/組織/所有者＝CLAUDE.md「Pipeline設定」RLS構成メモ）に合わせてシード**しないと自分のセッションから見えない ③検証後 cleanup。「実機が一致しない」時はまず**ブラウザのハードリロード＋dev server最新性＋データの実在（＋RLSスコープ）**を切り分ける（コード結論を急がない）。

### 🖥 app固有の確認方針（APP_LAYOUT_NOTES 準拠）
対象アプリの UI/ロジックは**原則ブラウザ（{{DEV_URL}}）で確認する**。**ネイティブ依存・LINEアプリ内固有（プッシュ通知の実機体裁・カメラ・APNS/FCM・Flexメッセージ体裁・友だち追加導線・課金の実機フロー 等＝APP_LAYOUT_NOTES の該当項目）**は**『⚠実機確認が必要』**とステップに注記する。**現段階はブラウザ確認で承認可**、実機固有は**NGにせず⚠保留として記録**（§3-B の指摘ではなく「⚠実機確認 残」として本文にメモし、ブラウザで確認できた範囲で承認してよい）。
**外部送信（LINE配信/メール/push 等＝APP_LAYOUT_NOTES の送信媒体）を伴う確認は必ず「自テナント・自分宛・隔離・dry-run優先」**で行い、確認後 cleanup する。他テナント・実顧客への実配信は絶対にしない。

---

## §2 3経路のナビ（🤖 は畳む / 🔴 は合否を聞く / 🤝 は合否を聞かない）

> **契機（2026-08-07 運用者の実地フィードバック）**: 「**スクリーンショットを確認する、はやっぱり微妙であんまり意味がない**」
> 「**開発者である僕が実際に触る機会は従来通りあった方がいい**」「**単純な CRUD や単純なページ遷移は E2E で十分**」。
> 設計は `plans/20260807-test-and-contact-split.md`。**合否（テスト）と 接点（レビュー）を分ける**。
> 2026-08-06 の駆動スクリプト（到達だけして人がスクショを見る）は**廃止**——
> **スクショは両方の劣化版**で、検証は E2E に劣り、接点は手で触るに劣る。

`run.md` の🧪手順は **🤖 / 🔴 / 🤝** の3つで書かれている。`/review` はそれぞれ別の扱いをする。

### 🤖 自動で担保 — **1行に畳む。ステップにしない**
```
✅ 自動で担保: E2E N本 green（<spec名>, <spec名> …）／build・typecheck・RLS監査 も緑
```
これだけ。**合否は機械が出しているので人に聞かない。** 中身を1つずつ見せない。

### 🔴 人がテストする — 従来どおり合否を聞く（ただし件数は激減する）
```
🔴 k/m
📍 場所: [<完全URL>](<完全URL>)   ← 必ずクリック可能な Markdown リンクで出す
🖱 操作: <何をする>
✅ 期待: <こうなれば合格>
（⚠ Gemini指摘の該当があればここに「特にここを見て」を併記）

→ 「OK」/「NG（内容）」/「skip」/「中断」 で答えてください
```
- **🔴 が0件のチケットは正常**（`run.md` の11カテゴリに当てはまらなければ書けない）。0件なら「🔴 なし（理由）」と出して 🤝 へ進む。
- 🔴 の各ステップは **11カテゴリのどれか**であるはず。**カテゴリ外のものが書かれていたら、それ自体を指摘する**
  （＝spec を書くべきものが人力に流れている合図）。

### 🤝 触ってほしいところ — **合否を聞かない**
```
🤝 触ってみてください
📍 [<完全URL>](<完全URL>)

気になったことがあれば教えてください（無ければ「特になし」で次へ進みます）
```
- **手順も期待結果も出さない。** 出すとチェックリストに戻り、契機の不満（単純なCRUDを一個ずつ確認させられる）に逆戻りする。
- **OK/NG を聞かない。** 目的は合否ではなく理解・接点（第10条・第7条）。
- 「特になし」で次へ。**言われたことは §3 の記録に必ず載せる**（合否ではないが、発見は残す）。
- 🤝 が🧪手順に1つも書かれていなかったら、**それ自体を指摘する**（接点ゼロは `run.md` が禁じている）。

### CC はスクリーンショットを人に見せない
**画面を撮って「これでOKですか」と聞かない。** 人には**実際に触ってもらう**（🤝）か、
**合否が要るなら自分で操作してもらう**（🔴）。スクショは検証にも接点にもならない。
（CC が自分の確認のために撮るのは自由。人への提示物にしないだけ。）


### ★指摘・追加要望は着手前に選ばせる（2026-07-13追加・同日中に基準を厳格化）
NG・追加のUX要望（バグとは言い切れない改善要望も含む）が出た時、**CCがそのまま調査・実装に入るのはデフォルトで禁止**。ファイル横断の実装・新規state/CSS構造の変更・原因調査を伴うものは全て、着手前に必ず一言で選ばせる:
```
これは (A) このセッションで今すぐ調査・修正する／(B) 内容を記録して次の/runに送る、どちらにしますか？
```
即実行してよい「軽微」の基準は非常に狭い：**1ファイル内・1〜数行・既存の値/文言/条件を書き換えるだけ**（例: 色コード変更、文言修正、閾値変更）に限る。**複数ファイルにまたがる変更・新規CSS構造/コンポーネント分割・inputをtextareaに変える等の要素構造変更は「軽微」ではない**＝実装量の多寡に関わらず必ず選ばせる（sido /reviewセッションで「textarea化+自動リサイズ+送信者分離」の3点セットをCCが軽微と判断して即実装したが、複数ファイル修正で時間がかかりユーザーから明示的に指摘された実例あり）。**判断に迷ったら必ず(A)/(B)を聞く側に倒す**（レビューが長時間の無断作業で止まるのを防ぐのが目的）。
(B)を選んだら、§3-Bの「(2) 後で /run に直させる」と同じ扱い（レビュー待ち→進行中、指摘callout追記）にして次のタスクへ進む。

---

## §3 判定とステータス反映

全ステップ終了後、結果を集計して人間に最終確認:

### A. 🔴 が全部OK（指摘なし）＝レビュー承認
> **🤝 は合否ではないので承認判定に入れない。** 「特になし」でも「気になった点あり」でも、
> 承認を決めるのは 🔴 の結果だけ（🤝 で出た指摘は §3-B と同じ扱いで記録し、進行を妨げないものは次の `/run` へ回す）。
> **🔴 が0件のチケットは「🤝 を触ってもらった時点で承認可」**（11カテゴリに該当が無い＝人が合否を出すべき点が無い）。

- ステータスを **レビュー待ち → 本番待ち** に反映。**必ず REST PATCH で更新し、反映を再確認**する（MCPの`update_properties`はstatus取りこぼし実績あり）:
  ```bash
  node -e '/* .env の NOTION_TOKEN で */ fetch("https://api.notion.com/v1/pages/<id>",{method:"PATCH",headers:{Authorization:`Bearer ${TOKEN}`,"Notion-Version":"2025-09-03","Content-Type":"application/json"},body:JSON.stringify({properties:{"ステータス":{status:{name:"本番待ち"}}}})})'
  ```
  （実体は対話中にCCが組み立てて実行。更新後 GET で `本番待ち` を確認。）
- 本文に **`> [!note] ✅ 人力レビュー承認（CC nav / 日付）: 🔴 n件OK ／ 🤝 触った（<触った画面>）。reviewer=人間。本番待ちへ。`** を追記。
  **★内訳を必ず書く。** 「全ステップOK」だけだと、後から「人が全部見た」に読み替えられる（早期完了トークン）。
  🔴 が0件なら `🔴 0件（該当カテゴリなし）／🤝 触った` と書く。🤝 で指摘が出たならその要旨も1行残す。
- 1行サマリ（「①〈タスク〉→本番待ち（承認）」）を出して次のタスクへ。

### B. 指摘あり（NGステップが1つ以上）
- 本文に **`> [!warning] 🔴 レビュー指摘（CC nav / 日付）`** callout を追記し、**NGステップ・期待・実際・場所URL**を箇条書きで残す。
- 人間に選ばせる:
  - **(1) 今すぐ直す** → CCがそのまま修正に入る（featureブランチ特定 or dev上で修正→ゲート緑→devへ→再度この§2から）。
  - **(2) 後で /run に直させる** → ステータスを **レビュー待ち → 進行中** に戻す（REST）。`🤖AI実行中`タグは付けない（人手再開ではないが、/runのstep1が痕跡=featureブランチ/指摘calloutで再開できる）。指摘calloutが直し先。
- どちらでも本番待ちには上げない。1行サマリ（「①〈タスク〉→指摘N件（(1)修正中/(2)進行中へ差戻し）」）。

### C. 中断
- そのタスクは**レビュー待ちのまま**（途中までのOK/NGは本文にメモ）。次タスクに行くか終了するかを人間に確認。

---

## §4 次のタスク → 枯渇 or 中断まで繰り返し

- §1〜§3 を**レビュー待ちが尽きるまで**優先度順に繰り返す（1件終えるごとに次を提示。人間が「終了」と言えば止める）。
- 全件終わったら**総括**: 承認→本番待ちにした件数／指摘で差し戻した件数／残レビュー待ち。
- **本番待ちが溜まったら案内**: 「本番反映は /ship（エピック単位でまとめ可・🔴高/🧱土台は本番スモークしてから・🟢はノールック可）」。**/review は ship をしない**。

---

## テストURL早見表

| 環境 | URL | 用途 |
|---|---|---|
| **ローカル(dev確認・既定)** | {{DEV_URL}}（APP_LAYOUT の起動コマンド） | レビュー待ちの dev コードを動かす。テスト テナント・ローカル Supabase（LOCAL_STACK のポート） |
| 本番（ship後のみ） | {{DEPLOY_PLATFORM}} の本番URL（CLAUDE.md「Pipeline設定」記載） | ship 後の本番スモーク用。レビュー段階ではローカル列のみ有効 |

- 画面パスはプロジェクト固有（CLAUDE.md「Pipeline設定」APP_LAYOUT_NOTES の画面パス例）。🧪手順の指す画面に応じて**完全URLに具体化**して提示する。
- 可変部（各種ID）が要る時は、ローカルにテストデータを用意して**実URLを埋める**（人間にIDを探させない）。実機固有導線は `⚠実機確認` 注記（§app固有の確認方針）。

---

## 厳守
- **push / deploy / db push / 本番migration適用 / ship はしない**（本番反映は /ship 専用・人間ゲート）。
- ステータス更新（レビュー待ち→本番待ち / →進行中）は **REST PATCH で実行し、更新後に必ず再確認**（MCP update_properties は status を取りこぼす実績あり）。
- **人間がOKと言ったステップだけ**合格にする。CCが勝手に「OK」にしない。自動で担保済みの項目は省略を明示。
- **実外部送信（LINE配信/メール/push 等）は自テナント・自分宛・隔離のみ**。他テナント/実顧客への実配信は絶対にしない。dev 段階は dry-run 優先。
- ⚠️要注意（Gemini指摘等）は**隠さず最初に提示**し、人間が承知の上で承認するかを確認する。
- 対象は自案件（`.env` の `BACKLOG_PROJECT_ID`）のレビュー待ちのみ。
- **時間のかかりそうな指摘・追加要望は、調査に飛びつく前に「今すぐ直すか／次の/runに送るか」を必ず選ばせる**（§2「★時間のかかる指摘・追加要望は着手前に選ばせる」）。軽微な修正は即対応してよいが、原因調査が要るものは判断を人間に預ける。

---

## Phase 5 改修（2026-07-24）— 自動テスト前段・進行を妨げない指摘は差し戻し
- **前段「自動テストでカバー済みか」判定**: 各人力チェック項目について、対応する E2E/spec が既に存在し green なら**人力項目から除外**（人の帯域を空ける）。カバー有無は `products/<name>/CLAUDE.md` の E2E baseline / spec で判定。
- **進行を妨げない指摘はチケット追記で次回 run へ差し戻し**（T11 と整合）: レビュー中に見つかった「今の承認は妨げないが直したい」指摘は、承認をブロックせずチケット追記して /run へ回す。NG（承認を妨げる）だけがブロック。
- 摩擦は `log-friction.mjs`（型・発生元=/review）。
