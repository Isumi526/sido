---
description: 自走ループ（リスク2レーン/緊急/エピックship/Gemini二重レビュー）
argument-hint: "[--dry-run]"
disable-model-invocation: true
---

# /run — 自走 ＋ リスク2レーン ＋ 緊急プリエンプト ＋ エピックship ＋ Gemini二重レビュー（統合版）

> **【正本（~/cc-pipeline）・プロジェクト中立版】** このファイルは固有値をハードコードしない正本。各プロジェクトへ実ファイルとして展開し、固有値は各リポの **CLAUDE.md「Pipeline設定」** と `.env` で解決する。依存値（APP_LAYOUT / TYPECHECK / BUILD / TEST / PLAYWRIGHT_PROJECTS / LOCAL_STACK / MIGRATIONS_DIR / DEPLOY_PLATFORM / DEV_URL / PROD_BRANCH / NOTIFY_PREFIX 等）の `{{...}}` はその参照プレースホルダ。通知は `scripts/notify-humanball.mjs` が `.env` の `NOTIFY_PREFIX` を task 名頭に自動付与する。

> **更新履歴**
> - 2026-06-14 統合再設計：自走ループ＋自動テストゲート（動的検出）＋ディスパッチャを1本に集約。旧 `--advance` / `AUTO_ADVANCE` を削除。
> - 2026-06-18 リスク2レーン再構築：低リスク=dev自動マージ→本番待ち（レビュー不要）／中・高・土台=テスト緑→[🔴高・🧱土台はGemini二重レビュー]→レビュー待ち。緊急プリエンプト・エピック単位ship通知・棚卸し統合を明文化。**通常停止フラグ `--max-items` / `--max-minutes` を撤去**（残すフラグは `--dry-run` と回路遮断 `MAX_WALL` のみ）。本番待ちは「ship待ちバッファ」として存続。handoff（ship.md / next.md）は不変。
> - 2026-06-19 §リスク・エピック自動判定の `エピック` 付与に明文化：**どの既存エピックにも収まらない時だけ新規エピックを作成してよい**（乱立は避け既存への寄せを優先）。
> - 2026-06-28 **(1) ドレイン徹底**：レビュー待ち/本番待ち/**マージ待ち（dev堆積）が何件溜まっても停止理由にしない**を全面強化。停止は「要件定義済み（実装可能）の枯渇」「本番ゲート」「異常（回路遮断）」の3つだけ。ディスパッチャの閾値は**通知トリガーであって停止条件ではない**ことを明記。 **(2) 緊急レーン分離**：`優先順位=緊急` は **main派生（origin/{{PROD_BRANCH}}基点）の hotfix ブランチ**で実装し（dev堆積から隔離）、ゲート緑まで自走→**本番待ち＋🚨緊急通知**。push/PR/Merge は人（/ship）。これで「ドレイン徹底でマージ待ちが膨らんでも、緊急だけは独立して即shipできる」状態を担保。

CLAUDE.md「自走ポリシー」に従い、**自プロジェクト（`.env` の `BACKLOG_PROJECT_ID`＝案件 relation で絞った page_id のみ）**のバックログを、毎ループ最優先1件ずつ前進させる単一ループ。**「要件定義済み（実装可能）が枯れるまで」ノンストップで自走**（残予算でも、レビュー待ち/本番待ち/**マージ待ち（dev堆積）が何件溜まっても止めない**）。技術・手続き判断は既定ポリシーで自決し、**人間の業務判断だけ要回答にパーク**（止まらず次へ）。**唯一の人間ゲートは本番反映（push / deploy / ship）と レビュー待ち→本番待ち 昇格**。run 末尾で**ディスパッチャ**が「決断の瞬間」だけ通知する。

> **ドレイン徹底（最重要・2026-06-28）**：このループは **要件定義済みが空になるまで止まらない**。レビュー待ち・本番待ち・dev のマージ待ちが大量に積み上がっても、それは正常（dev未push＝本番影響ゼロ）であり**停止・中断・確認の理由にしない**。マージ待ち膨張のリスクは「緊急チケットを main派生で独立shipできる」ことで吸収する（下記§3-4①・§緊急レーン）。

- 前提資産: バックログ data_source `{{BACKLOG_DS_ID}}`（.env／Notion-Version `2025-09-03`）。**A'方式＝自 page_id のみ**を対象。
  ステータス: 未整理→要回答→要件定義済み→進行中→レビュー待ち→本番待ち→完了（＋保留）。
  プロパティ: `優先順位`(緊急/高/中/低) / `リスク`(🔴高/🟡中/🟢低 Select) / `土台`(Checkbox) / `案件名`(relation) / `エピック`(=shipグループ Select)。
- **パラメータ（リポ直下 `.env` から読む）**: `AUTO_MERGE_TARGET=dev`（自動マージ先）／`AUTO_TIER=低`（レビュー不要で本番待ちへ自動マージできる最大リスク階層）／`GEMINI_REVIEW_API_KEY`・`GEMINI_REVIEW_MODEL`（二重レビュー用・**クライアントへ明示的に渡す＝SDK自動検出に頼らない**）。回路遮断 `MAX_WALL=180`分。

引数: `$ARGUMENTS`
- `--dry-run` … 盤面・各件の遷移予定（どのレーンへ）・各ゲートの実行有無・Gemini二重レビュー予定・ディスパッチャ通知予定を**提示のみ**（Notion更新・コード変更・通知・ブランチ・migration適用・テスト実行・マージを一切しない）。副作用ゼロ。
- ※ `--max-items` / `--max-minutes` は**撤去**。通常停止は「キュー枯渇」、回路遮断は `MAX_WALL=180分` と「連続前進ゼロ K=3」のみ（§6）。

人ボール/承認の文面は CLAUDE.md「人ボール報告の必須フォーマット（結論ファースト）」に従う。

---

## ステータス遷移（AUTO_MERGE_TARGET=dev）

```
未整理 ──振り分け──▶ 要件定義済み / 要回答(業務判断)
要件定義済み ─実装+テスト全緑─┐
   ├─【低リスクレーン】(リスク ≤ AUTO_TIER) ─▶ dev自動マージ ─▶ 本番待ち（レビュー不要・ship待ち）
   └─【中/高/土台レーン】 ─[🔴高・🧱土台は Gemini二重レビュー]─▶ レビュー待ち ─(人間レビュー)─▶ 本番待ち
本番待ち（無レビュー低リスク＋レビュー済み中高が混在）─人間がエピック単位でまとめてship─▶ 完了
   （🟢低リスク分はノールック／🔴高・🧱土台分は本番レビューしてからship）
```
- **緊急**（`優先順位=緊急`）はリスク不問で**最優先割り込み**。**ただしマージ経路は dev ではなく「main派生（origin/{{PROD_BRANCH}}基点）の hotfix ブランチ」**で実装し（＝dev堆積から隔離して独立shipできるようにする）、ゲート緑まで自走 → リスク不問で **本番待ち** に置き **🚨緊急通知**。push/PR/Merge は人（/ship）。緊急は速さ最優先のため、🔴高でも レビュー待ち を経由せず本番待ち直行（ただし Gemini/RLS等のゲートと🧪人力チェック手順は通常どおり実施＝品質は落とさない／本番レビューは /ship 時に人が行う）。詳細は §緊急レーン。
- ※将来 `AUTO_MERGE_TARGET=prod` 切替時のみ：低リスクは本番待ちを経ず **完了** まで自動（現行 dev では必ず本番待ちで一旦停止＝本番影響ゼロ）。

---

## /run 統合フロー（自 page_id 内・枯渇まで自走）

### 0. プリフライト＋自己修復＋緊急監視（ループ前に1回＋毎周回の冒頭）
- **自己修復**: 自案件で「🤖AI実行中」タグが残っているものを全部外す（前回クラッシュの取り残し）。`--dry-run` は列挙のみ。
- **local接続 assert（本番E2E禁止）**: **{{LOCAL_STACK}}=none のプロジェクトはローカルスタックが無いため、この local接続 assert 自体をスキップ**（E2E/DB検証も実体が無ければ走らせない）。ローカルスタックがある場合のみ：E2E/DB検証を使う前に**毎回**、起動中の dev/stack が **local 接続**かを確認（配信される Supabase URL / anon key が **local（{{DEV_URL}} / 127.0.0.1:54321 等）**を指し、**本番プロジェクトの URL / JWT ではない**こと）。本番接続を検出したら**検証を止め**、`.env.local` で local 起動/再起動してから回す。**本番に対して E2E を絶対に走らせない**。例: `curl -s {{DEV_URL}}/ | grep -oE '127\.0\.0\.1:54321|<本番SUPABASE_URLの一部>'` で local 側に一致することを確認。Docker/`supabase start` が落ちていれば起動。聞かない。
- **緊急監視（常時）**: 毎周回の冒頭で `優先順位=緊急` の未着手（要件定義済み/未整理）が無いか見る。あれば§2でプリエンプト。

### 1. 進行中（再開可能なCC痕跡あり）を先に完了
featureブランチ/設計書/🤖タグ のいずれかがある進行中を再開：実装→自動テストゲート(§5)→**当該リスクのレーンで着地**（低→§4／中高土台→§3）。痕跡が無い＝人手の進行中は触らない。

### 2. 未整理の棚卸し（統合・速い・実装しない）
各件を内容から判定し、**1件処理ごとに盤面先頭へ戻る**。判定時に以下を**まとめて自動付与**（リスク/土台は必ず該当 Select/Checkbox プロパティ値に入れる・本文/独自タグ禁止）：
- **【優先度】**（緊急/高/中/低）・**【エピック】**（shipグループ）・**【リスク】**（🔴高/🟡中/🟢低）・**【土台】**（後続が依存するか）。判定基準は §リスク・エピック自動判定。
- **(a) 業務判断が必要**（業務ルール/要件が曖昧・本文が空/不足）→ **要回答**化（A/B/C＋推奨）し**パーク**、止まらず次へ。
- **(b) 要件化可能** → 薄いストーリー＋AC＋scope を書き、上記4プロパティを付与して **要件定義済み**。

### 3〜4. 要件定義済みを実装（① 緊急を最優先割り込み → ② 残りは**全体をリスク低い順（🟢→🟡→🔴）** → 同リスクは優先度高い順→作成古い順）
> **順序の主キーは「リスク」**（優先度ではない）。狙い＝バックログが溜まりがちなので、**初速で低リスクの軽い案件を全体から一気に片付けて queue を速く減らす**（🟢低は自動マージ→本番待ちで即積める）。
> - **① 緊急（`優先順位=緊急`）だけ特別扱い**：リスク不問で**最優先割り込み**（業務停止級＝速さより重要）。**実装は dev ではなく main派生（`git fetch origin {{PROD_BRANCH}} && git switch -c hotfix/<slug> origin/{{PROD_BRANCH}}`）の hotfix ブランチ**で行い、ゲート緑まで自走 → **本番待ち**（リスク不問・レビュー待ちを経由しない）＋ **🚨緊急通知**。**push/PR/Merge はしない（人が /ship で行う＝本番ゲート）**。狙い＝マージ待ち(dev)がどれだけ膨らんでも、緊急だけは clean な main から分岐して**独立して即shipできる**（dev堆積に巻き込まれない）。詳細手順は §緊急レーン。
> - **② それ以外は優先度を跨いで🟢低を全部 → 次に🟡中を全部 → 最後に🔴高/🧱土台**、の順で実装する。同リスク内は 優先度(高>中>低)→作成古い順。
> - 重い🔴高/🧱土台は、軽い物を捌き切ってから着手＝人のレビュー負荷も後ろ倒しにできる。
> - **【厳守】🟡中が1件でも残る限り🔴高/🧱土台へ進まない**。盤面の `リスク` プロパティ分類を**尊重**し、🟡中を「金額/anonっぽいから実質🔴高」と**着手前に勝手に昇格してスキップするのは禁止**（昇格は §隠れリスク発見ルールどおり**実装に着手して実コードで判明した時だけ**）。曖昧な🟡中は §2(b) 要件化 or §2(a) 要回答で前進させる（黙ってスキップしない）。
> - **【厳守】1件だけ実装して止めない＝レビューはバッチで積む**。🟡中レーンを順に捌き、**できれば同じ `エピック` の案件をまとめて**レビュー待ちに積む（人が /review でエピック単位でまとめて確認できる状態にする）。レビュー待ちが溜まる事自体は停止理由ではない（§6①の枯渇 or §6③の異常まで自走）。
1件取り、`進行中`＋🤖タグにして実装：
- **a. 実装**：既存パターン（近い画面/関数/テスト）に準拠。タスクに「**決定と理由**」を1行記録。追加のみDDL（ADD COLUMN/CREATE TABLE/CREATE INDEX 等）は **作成＋local適用**（`db reset` 等）。**本番適用はしない**。実装中に業務判断が要ると判明したら**要回答**化してパーク・次へ。
- **b. 【自動テストゲート】今存在する自動テストを“全部”通す**（動的検出・§テストゲート）。緑でなければ **blocked**（ステータス=保留＋失敗内容を記録・🤖外す）→ 次へ（run 全体は止めない）。
- **c. レーン分岐**（`リスク` プロパティで判定）:

  **§4 低リスクレーン（`リスク` ≤ `AUTO_TIER`＝🟢低）**
  - テスト全緑を確認 → `AUTO_MERGE_TARGET`（=dev）へ**自動マージ**（ローカルマージ・**push しない**）→ ステータス=**本番待ち**・🤖外す。**レビュー不要**（ship待ちバッファに積むだけ＝本番影響ゼロ）。📋ダイジェストは軽め（変更概要＋env前提＋推奨shipグループ）で可。

  **§3 中/高/土台レーン（🟡中・🔴高・🧱土台）**
  - **c'. RLS/anon 機械監査（migration差分がある時だけ・軽め）**：差分に `supabase/migrations/` 追加/変更を含む場合のみ `node scripts/rls-audit.mjs --assert --json` をローカル(適用済)に対して実行。**🔴LEAK** があれば 📋に🔴で載せる（gateは止めない＝レビュー待ちには上げ人間判断に委ねる）。
  - **c''. 【Gemini二重レビュー】（🔴高・🧱土台のみ必須／🟡中は任意）**：`node scripts/independent-review.mjs "<diff範囲>" --json --ticket-id <pageId> --ticket-file <要件md>` を実行（Claude非依存の Gemini が `.kody/rules/` ＋ チケット要件で diff をレビュー＝**実装AIと別AIによる二重チェック**）。**findings／verdict／riskClass を 📋ダイジェストに追記**。**ブロッカー級（未accept critical / 🔴LEAK）は自分で修正を試行**し、緑化できたら再レビュー。**修正不能なら「⚠️要注意」を 📋に明示**（block はせず人間verdictに委ねる＝スクリプトが Notion callout＋通知）。`GEMINI_REVIEW_API_KEY` 未設定なら**スキップした旨を📋に明記**（gate扱いにしない）。
  - **d. 昇格**：`リスク`(🔴/🟡/🟢 Select)・`土台`(該当 Checkbox=true) を**プロパティに直接セット**し、本文に **📋レビューダイジェスト**（§ダイジェスト・c'/c''結果を含む）＋ **🧪人力チェック手順**（§人力チェック手順・**中/高/土台は必須**）を書いて、`AUTO_MERGE_TARGET`(=dev)へ**devマージ**（push しない）→ ステータス=**レビュー待ち**・🤖外す。**review→本番待ち 昇格はしない**（人間が行う）。※ここで書いた🧪手順は人間が本番待ちへ昇格した後もそのまま ship 前の人力確認に使う。
- **e. 本番に触れない**。dev 未push前提で積む。

### §緊急レーン（`優先順位=緊急` のみ・main派生で dev堆積から隔離）
通常レーン（dev基点→devマージ→レビュー待ち/本番待ち）と分け、**緊急だけは clean な main から分岐**して独立shipできる形にする。これにより「ドレイン徹底で dev のマージ待ちがどれだけ膨らんでも、緊急は巻き込まれず即shipできる」を担保する。

手順（毎ループ冒頭の緊急監視で検知したら、他の何より先に）:
1. **隔離ブランチ**：`git fetch origin {{PROD_BRANCH}}` → `git switch -c hotfix/<slug> origin/{{PROD_BRANCH}}`（**dev ではなく origin/{{PROD_BRANCH}} 基点**。作業中の dev 変更があれば commit/stash してから）。
2. **着手記録**：ステータス=`進行中`＋`🤖AI実行中`。
3. **実装**：通常と同じ品質。追加のみDDLは作成＋local適用（本番適用しない）。実装中に業務判断が要ると判明したら **要回答パーク**（hotfixブランチは破棄 or 残置メモ）し dev へ戻って次へ。
4. **ゲート**：§テストゲート（動的検出・全部緑）。migration差分あれば `rls-audit`。**🔴高/🧱土台は Gemini二重レビュー必須**（緊急でも品質は落とさない）。緑にできなければ blocked（保留）にして dev へ戻る。
5. **コミットのみ（★pushしない）**：hotfixブランチに commit。**push / PR / Merge はしない**（＝本番ゲートは人＝/ship）。
6. **着地**：📋レビューダイジェスト＋🧪人力チェック手順を本文に書き、**リスク不問で `本番待ち`**（レビュー待ちを経由しない＝速さ優先。🔴高の本番レビューは /ship 時に人が実施）。📋に「**緊急hotfix・ブランチ=`hotfix/<slug>`（main派生・未push）**」を明記（/ship が拾えるように）。
7. **🚨緊急通知**：`notify-humanball.mjs --kind 要対応`（または緊急用文面）で「緊急hotfix `<タスク>` ゲート緑・本番待ち。/ship で push→{{PROD_BRANCH}}宛PR→Merge を」。
8. **戻る**：`git switch dev`（or 元の作業ブランチ）して通常ループを継続（緊急は1件ずつ。複数あれば都度この手順）。

> /ship 側の対応：本番待ちのうち **ブランチが `hotfix/*`（main派生）** のものは、dev経由ではなく **その hotfix ブランチを push → {{PROD_BRANCH}}宛PR → Merge** で独立shipする（dev堆積と無関係に出せる）。通常の本番待ち（dev上）は従来どおり dev→PR→main。

### 5. （任意）preview/staging 再検証
preview/staging が構成済みならそこへデプロイし同じスイートを再実行（**無ければスキップ**）。

### 6. 停止条件
- **①枯渇（＝正常終了・唯一の通常停止）**：**未整理=0 かつ 要件定義済みに「実装可能な残り」が無い**（全件が レビュー待ち/本番待ち/要回答/blocked(保留)/完了 に収束）。**レビュー待ち・本番待ち・マージ待ち（dev堆積）は何件溜まっても停止理由にしない**（dev未push＝本番影響ゼロ。マージ待ち膨張のリスクは「緊急の main派生独立ship」で吸収済み）。**ディスパッチャの閾値（レビューバッチ等）は“通知トリガー”であって停止条件ではない**——閾値超えでも /run は止めず要件定義済みを掘り続ける。
- **②本番マージ要求**：人間の push/ship/昇格 を要する地点。CCは実行せず通知して待つ。
- **③異常（回路遮断）**：**連続前進ゼロ K=3**／**壁時計 MAX_WALL=180分**／**プラン枠で落ちた**。停止時は**枯渇か異常かを明記**。API課金へ勝手に流れない。
- 上記以外は**ノンストップ**。**「マージ待ち/レビュー待ちが多いから一旦区切る」は禁止**（①の枯渇まで掘り続ける）。
- **【厳守・止まる理由にしてはいけないもの】**：以下を理由に停止・中断・「続けますか？」と確認するのは**禁止**。①〜③の正規の停止条件**以外では必ず次の1件へ自動で進む**：
  - 「セッション/会話が長い」「出力(トークン)が多い」「ターンが長くなった」→ **止めない**（長さは停止条件ではない）。
  - 「次の案件が大きい/重い」→ 大きくても着手する（重い🔴高は§3の手厚い工数で。業務判断が要ると判明したら要回答パークして次へ）。
  - 「レビュー待ち/本番待ち/マージ待ち（dev堆積）が溜まった/区切りが良い」→ §6①の枯渇まで止めない（堆積は正常・本番影響ゼロ。膨張リスクは緊急のmain派生独立shipで吸収済み）。
  - **人間に「続けていいか」を聞かない**（[[feedback_run_no_confirmation]]）。1ユニットごとの1行報告はしてよいが、承認は求めず即次へ。
  - 報告で「一旦区切る/ここまで」と書くのは、①枯渇 か ②本番ゲート か ③異常 の時だけ。それ以外で区切らない。

### 7. ディスパッチャ自動発火（フロー最後に1回・どの停止理由でも）
**フローの最後に `node scripts/dispatcher.mjs`（非dry-run）を1回だけ呼ぶ**。flock でガード済（二重起動は後発即終了）。delta-only（新規トランジションが無ければ無音）。`--dry-run` 時はディスパッチャも `--dry-run` で。

```
preflight()             # 0: local接続assert＋自己修復(🤖)＋緊急監視
finishResumableInProgress()   # 1
groomInbox()            # 2: 優先度/エピック/リスク/土台 を付与し (a)要回答パーク / (b)要件化
noProgress = 0
while implementableReady():     # 3-4 / 6①(枯渇で抜ける) ※レビュー待ち/本番待ち/マージ待ちが何件でも止めない
    if elapsed()>=MAX_WALL(180m) or noProgress>=3: break        # 6③（堆積は break 理由ではない）
    item = preempt緊急() ?? top(リスク低い順🟢→🟡→🔴 を主キー, 同リスクは優先度高>中>低→作成古い順)   # ①緊急だけ割り込み・②残りは全体を低リスク順
    setInProgress(item)+🤖

    if priority(item)==緊急:                                      # ★緊急レーン（main派生・dev堆積から隔離）
        sh('git fetch origin {{PROD_BRANCH}} && git switch -c hotfix/<slug> origin/{{PROD_BRANCH}}')
        implement(item)                                          # a
        if needsBizDecision: parkAsAnswer(A/B/C+推奨); notify; 🤖外す; backToDevBranch(); noProgress=0; continue
        if !runAllAutoGates(item):
            block(item, failLog); 🤖外す; backToDevBranch(); noProgress+=1; continue
        if migrationDiff(item): rlsAudit(item)                   # c'（緊急でも品質ゲートは通常どおり）
        if risk==🔴高 or dodai: geminiDoubleReview(item)         # c''
        commitOnHotfix(item)                                     # hotfixブランチにコミット（★pushしない）
        setRiskProp(item); setDodaiProp(item); writeDigest(item)+writeManualCheck(item)
        setProdWaiting(item)                                     # ★リスク不問で本番待ち（レビュー待ちを経由しない）
        notify緊急(item, 'main派生hotfix・ゲート緑・本番待ち。/ship で push→PR→Merge を')
        🤖外す; noProgress=0; backToDevBranch(); continue        # devブランチへ戻って通常ループ継続

    implement(item)                 # a 決定理由1行・追加DDLはlocal適用
    if needsBizDecision: parkAsAnswer(A/B/C+推奨); notify; 🤖外す; noProgress=0; continue
    if !runAllAutoGates(item):      # b 動的検出・全部実行
        block(item, failLog); 🤖外す; noProgress+=1; continue
    if risk(item) <= AUTO_TIER:                                   # §4 低リスクレーン
        autoMergeTo(AUTO_MERGE_TARGET); writeDigest(item, light)
        setProdWaiting(item)        # 本番待ち（レビュー不要・push しない）
    else:                                                        # §3 中/高/土台レーン
        if migrationDiff(item): rlsAudit(item)                   # c'
        if risk==🔴高 or dodai: geminiDoubleReview(item)         # c'' 別AI二重レビュー
        setRiskProp(item); setDodaiProp(item); writeDigest(item) # d
        mergeTo(AUTO_MERGE_TARGET); setReviewWaiting(item)       # レビュー待ち（push しない）
    🤖外す; noProgress=0
run(`node scripts/dispatcher.mjs`)  # 7 末尾1回・非dry（flock・delta-only）
report(枯渇/異常 / 盤面 / 本番待ち(🟢無レビュー・🔴🧱要本番レビュー) / レビュー待ち / blocked / 要回答パーク / shipゲート待ち)
```

---

## リスク・エピック自動判定（§2 棚卸し・§3d 付与の判定源）
- **`リスク`(Select)**：**🔴高**=schema/migration・共有util・データモデル・認証/RLS・課金・外部送信／**🟡中**=複数画面・複数ファイルの機能追加／**🟢低**=自己完結UI・文言・単一トグル・設定値。
- **`土台`(Checkbox=true)**：後続の未着手/要件定義済みが**この成果物に依存**するなら true（後続件名/page_idを📋の「依存」に記す）。
- **`エピック`(Select)**：ship単位。近い画面・機能群・既存エピックに寄せる（※エピック例はプロジェクト固有＝**Notion 側の実エピック値**に合わせる。代表例は各リポ CLAUDE.md「Pipeline設定」参照）。一意に決まらなければ近接既存エピックへ。**どの既存エピックにも収まらない時だけ新規エピックを作成**してよい（粒度＝ship単位。単発タスクのための乱立は避け、まず既存への寄せを優先）。
- **`優先順位`**：本文の緊急度語・依存・ユーザー直指示から（緊急＝障害/業務停止級、高＝近接の本命機能、中＝通常追加、低＝軽微）。
- いずれも**Select/Checkbox プロパティ値に直接セット**（本文・独自タグで代替しない）。判断が割れる業務的なものだけ §2(a) 要回答へ。

## リスク別 工数（effort）スケーリング（§3 実装の深さをリスクで変える）
**狙い**：🟢低は軽くして初速UP・🔴高は手厚く堅く出す。リスク階層に応じて CC の作業の深さを下表で変える（過剰実装も検証不足も避ける）。

| 観点 | 🟢低 | 🟡中 | 🔴高/🧱土台 |
|---|---|---|---|
| **着手前の調査** | なし（直接実装） | 該当ファイル確認 | **Exploreで全消費箇所をマップ**（直し漏れ/footgun回避） |
| **自動テスト** | build/typecheck のみ | build＋関連E2E＋**保存→再読込の永続/回帰1本** | build＋**境界値・二重計上等のedge E2E**＋全消費箇所green |
| **Gemini二重レビュー** | なし | 任意(runs=1) | **必須(runs=2〜3 union)** |
| **RLS/anon監査** | なし | migration差分時 | **必ず実行** |
| **CCの思考の深さ** | 速く決める | 標準 | **熟考・検算・複数視点で確認** |
| 人力レビュー(§/review) | ノールック（本番待ち直行） | 要点ステップ | **詳細ステップ＋デモデータ用意＋⚠承知確認** |
| ship(§/ship) | ノールック可 | 本番軽スモーク | **本番スモーク必須** |

**最低ライン（リスク不問）**：機能追加は最低でも「**保存→再読込で永続するか**」を1本は確認する（UI追加だけでDB制約/保存漏れを見逃さないため。例: enum/区分追加は CHECK制約・保存経路まで）。

### 隠れリスク発見＝工数昇格ルール（重要）
分類は着手前の見立てに過ぎない。**実装中に下記を発見したら、その場でリスクを上げて工数も上げる**（or §2(a)要回答パーク）。事故った"軽い実装"を防ぐ最後の砦：
※**昇格は「実装に着手して実コードで判明した時だけ」**。着手前の見立てで🟡中を🔴高扱いして**スキップするのは禁止**（盤面の分類を尊重し、低リスクから順に捌く＝§3-4 厳守）。判明したら下記で昇格する：
- **金額/原価/集計に触れる**（二重計上・按分・retroactive変更）→ 🔴高扱い＝edge E2E＋Gemini＋（業務判断なら）要回答。
- **データモデル/スキーマ前提が想定と違う**（リレーション無い・CHECK制約・enum固定 等）→ migration要否を再判定し🟡→🔴へ。
- **共有util/複数消費箇所**（金額/集計/外部送信を複数箇所が消費する横断utility ※実体はプロジェクト固有の共有モジュール＝CLAUDE.md「Pipeline設定」APP_LAYOUT 参照）→ チェックリスト全消費箇所を直す（直し漏れ防止）。
- **anon露出/認証/RLS/外部送信** に触れると判明 → 🔴高＋RLS監査＋accepted.yml追跡。
昇格したら Notion の `リスク` プロパティも上げ、📋ダイジェストに「着手時に判明し昇格」と1行残す。

## 自動テストゲートの動的検出（ハードコードしない＝有るものを全部）
**特定テストを名指ししない。実在ゲートを毎回スキャンして全実行**。命名規約に沿えば将来分も自動で取り込む。
1. **typecheck / build**: root と各 `apps/*/package.json` の scripts から `typecheck` / `build` を検出して全実行（{{APP_LAYOUT}}・具体コマンドは **CLAUDE.md「Pipeline設定」の TYPECHECK / BUILD** に従う。pnpm monorepo なら `pnpm -r typecheck` / `pnpm -r build` 等）。
2. **テストscript**: root と `apps/*` の scripts のうち **`/^test/` に一致する全部**（{{TEST}}・`test` / `test:e2e` / `test:unit` / 将来の `test:visual` 等。**TEST が none＝テストscriptが無ければこの段はスキップ**）。
3. **Playwright**: `playwright.config.*` があれば定義された **projects を全部**（{{PLAYWRIGHT_PROJECTS}}・未設定/playwright.config 無しなら全spec or **スキップ**）。関係 spec は緑を明示。
4. DB/E2E を要するものは §0 プリフライトで **local を保証**してから実行。
- いずれか緑でない→ そのアイテムは **blocked**（落ちた script 名と要約を記録）。「3回直しても緑にできない」も blocked。
- **既知の事前失敗（本変更と無関係）は回帰判定から除外**してよい（除外したものは📋に明記）。

## Gemini二重レビュー（別AIによる独立二重チェック）の呼び出し設計
- 実体は `scripts/independent-review.mjs`。**実装はClaude／レビューはGemini**の二重構成。**`GEMINI_REVIEW_API_KEY` を HTTPヘッダ `x-goog-api-key` でクライアントへ明示的に渡す**（`GEMINI_REVIEW_MODEL` も .env 由来。**SDK自動検出に依存しない**）。
- 入力＝`.kody/rules/*.md`（ルール）＋ `--ticket-file`（要件md：意図照合）＋ `git diff <範囲>`。出力＝JSON（`findings[]` / `accessRuleEnforced` / `verdict` / `riskClass` / `accepted`）。`.kody/accepted.yml` で既知許容を block 除外。
- 呼び出し（🔴高・🧱土台で必須）:
  ```bash
  node scripts/independent-review.mjs "<diff範囲>" --json \
    --ticket-id <NotionページID> --ticket-file <要件mdパス>   # /run が --dry-run の時は末尾に --dry-run
  ```
  - `<diff範囲>` は当該レーン作業の差分（例 `dev...HEAD` ＝コミット済み feature 差分。未コミットは先にコミット）。
  - **頑健化が要る重要差分は `--runs 2`**（2回実行して findings を union＝非決定性の保険＝文字通りの“二重”）。
- 結果の扱い：**未accept critical / 🔴LEAK＝ブロッカー級は自分で修正→再レビュー**。緑化できたら昇格。**修正不能なら 📋に「⚠️要注意（要人間verdict）」を明示**してレビュー待ちへ（スクリプトが Notion callout＋通知を自動で立てる）。`verdict` は📋に必ず転記。

---

## 📋 レビューダイジェスト（レビュー待ち／本番待ちへ移す時＝必須）
当該ページに**必ず**書く（既存 reviewer note は置換）。バッチレビュー・エピックship 成立のための情報。
### 1. 本文に「📋レビューダイジェスト」callout
- **変更概要**(1〜2行・何を/どこに) / **リスク根拠** / **依存**（土台: 後続の件名・page_id ／ 乗っかり先行: 既マージ依存元） / **手動確認点**（E2Eで拾えない所＝英語表現/UX/通知本文/金額表示 等） / **env前提**（ship時に本番で presence check する項目） / **推奨shipグループ**（= `エピック`） / **二重レビュー結果**（🔴高・🧱土台: verdict・findings要約・RLS監査）。
- 低リスクレーンは軽め（変更概要＋env前提＋推奨shipグループ）で可。
### 2. プロパティに直接セット（タグ/本文で代替しない）
- **`リスク`(Select)** / **`土台`(Checkbox)**（基準は §リスク・エピック自動判定）。
### 3. 土台は「都度レビュー推奨」
- `土台`=true は通知＆サマリに「⚠️これは土台（後続N件依存）。早めにレビュー推奨」を1行。

---

## 🧪 人力チェック手順（中/高/土台 を レビュー待ち/本番待ち に置く時＝必須）
**中(🟡)・高(🔴)・土台(🧱)** をレビュー待ち（or 本番待ち）へ置くとき、Notion**本文**に「🧪人力チェック手順」callout を**必ず**付ける（📋レビューダイジェストの直下）。**目的＝人間に重複確認させず、自動で拾えない所だけを最短手順で見てもらう**。ディスパッチャ通知には**要約＋本文リンク**を載せ、**全文は本文**に置く。
**🟢低リスクは不要**（ノールックで本番待ち）。**粒度はリスクで変える：🔴高・🧱土台=詳細ステップ／🟡中=要点のみ**。

構成（この順で本文に書く）:
1. **✅ 自動で確認済み（緑・再確認不要）**：機能E2E / typecheck / build / RLS監査 等で**既に担保した項目**を列挙。→ 人間に**重複確認させない**ことを明示。
2. **👁 人間が見る点だけ**：番号付きで **【場所(URL/画面)→操作→期待結果】**。**自動で拾えない所に限定**＝見た目/UX/英語・日本語文言/主観的な業務ロジック/実外部配信（メール・LINEの実送信体裁 等）。
3. **🧪 使うテストデータ・前提**：どの**アカウント/テナント/データ**で再現するか（例: account=test、slug=…、シード手順 or 既存データ）。
4. **⚠ 重点確認（Gemini二重レビュー指摘があれば）**：c'' の findings / 「⚠️要注意」項目を**手順の該当ステップに組み込む**（reviewerが必ず触れる導線にする）。
5. **🚀 ship前スモーク（本番）**：本番反映直後に1分で見る確認。**外部副作用は必ず自分宛に**（メール→自分／LINE→自分／隔離テナントで実行し**確認後 cleanup**）。他人・全体配信は絶対しない。
6. **↩ 失敗時のロールバック手順 ＋ ⚠ ship必須env**：壊れた時の戻し方（revert/再デプロイ/データ戻し）と、**ship 時に本番へ要る env**（presence check 対象）を明記。

- `--dry-run` 時は「🧪手順を生成する予定」とだけ提示（本文は書かない）。

---

## ディスパッチャ（決断の瞬間だけ通知・`scripts/dispatcher.mjs`）
- /run 末尾から呼ぶ＋単体実行可（`node scripts/dispatcher.mjs [--dry-run]`）。判定材料は既存プロパティのみ（ステータス/リスク/土台/優先度/案件/エピック）。配信は既存 LINE human-in-the-loop（`notify-humanball.mjs`）。
- **dedup 必須**：`scripts/.dispatcher-state.json`（git管理外・キー=`page_id:status`）。毎実行プルーン（status変化/消滅キー除去）→差し戻し再入で再通知。**per-item スパム禁止＝1ダイジェスト集約**（🧱土台の即時のみ個別）。**初回統合**：プルーン後 state が空なら 🧱土台＋レビュー待ち＋要回答 を1通に統合して1回だけ送信。
- **緩急**: 🧱土台/🔴=即時、🟡/🟢=バッチ。

トリガー（**いずれも 要約＋本文リンクを載せ・🧪人力チェック手順の全文は本文**）:
1. **🧱土台がレビュー待ち入り** →【即】「🧱土台『X』レビュー待ち・後続N件依存・今レビュー推奨。🧪人力チェック手順は本文: <page url>」（個別なので本文リンクを直付け）。
2. **レビューバッチ準備**（土台以外のレビュー待ちが閾値超 or 🔴≥3 or max-age flush）→「レビュー可: 🔴a/🟡b/🟢c、要対応ビューへ（各本文に🧪人力チェック手順）」。
3. **エピックship可**（あるエピックの actionable が**全て本番待ち**）→【まとめ】「📦エピック『○○』ship可: N件(🔴a/🧱b/🟢c)。**🔴高・🧱土台は本番レビューしてからship／🟢はノールック可**。各本文の🧪手順でスモーク・env前提を本番確認」。
4. **要回答（業務判断）** → まとめて「判断待ちN件: ①…②…（A/B/C＋推奨）」。
5. **緊急検知** → 即時。 **異常/停止** →「drain停止: 理由・残件」。

```
# dispatcher.mjs（擬似コード・要点）
rows = queryDataSource(案件=BACKLOG_PROJECT_ID)          # 2025-09-03 / data_source {{BACKLOG_DS_ID}}
state = prune(load('.dispatcher-state.json'), rows)       # status変化/消滅キー除去
review = rows.where(status==レビュー待ち)
if state.empty: sendIntegratedDigest(🧱土台∪review∪要回答); save; return   # 初回統合
# 1) 🧱土台×レビュー待ち（即時・個別・要約＋本文リンク）
for it in new(review.where(土台)): notify(即,『🧱土台「it」後続依存・今レビュー推奨。🧪手順は本文: it.url』)
# 2) レビューバッチ（土台以外・閾値 or 🔴≥3 or max-age）
leaf = review.where(!土台); if (leaf>=THRESH or red(leaf)>=3 or maxAgeFlush) and new(leaf): notify(バッチ,『レビュー可 🔴a/🟡b/🟢c』)
# 3) エピックship（エピック単位・actionable が全て本番待ち → リスク内訳付き）
for epic, items in rows.groupBy(エピック):
    act = items.where(status in ACTIONABLE)
    if act && act.all(本番待ち):
        c = riskCounts(act); dodaiN = act.where(土台).count
        if new(act): notify(ship, epic, 『📦エピック「epic」ship可 N件(🔴c.red/🧱dodaiN/🟢c.grn)。🔴/🧱は本番レビュー後ship・🟢ノールック・env前提は各📋確認』)
# 4) 要回答（まとめて1通）
if new(要回答): notify(まとめ,『判断待ちN件 ①…②… A/B/C+推奨』)
# 5) 異常停止（run から理由を受けて）→ notify(『drain停止: 理由・残件』)
save(state)
```

---

## 人間ゲート
- **本番反映（remote push / 本番デプロイ / ship）のみ**人間。CCは明示OKまで実行しない。
- **レビュー待ち→本番待ち 昇格（=レビュー承認）も人間**（CCは中高土台を昇格させない）。
- **低リスクはレビュー不要**だが **本番待ちで ship 待ち**（dev止まり・push しない・本番影響なし）。
- `ship.md` は従来どおり **本番待ち のみ**拾う（未レビューの中高は構造上ここに来ない）。エピックship時、🟢はノールック・🔴/🧱は本番レビューしてから。
- それ以外（実装・local検証・migrationのlocal適用・**低リスクのdev自動マージ**・中高のdevマージ・レビュー待ち/本番待ち移動）は自走でOK。dev は未push・本番影響なし前提（{{DEPLOY_PLATFORM}}・dev はデプロイ対象外）。
- **緊急レーンも push しない**：`優先順位=緊急` は main派生 hotfix ブランチで実装し**ローカルでゲート緑＋本番待ち化まで**自走するが、**push / PR / Merge は人（/ship）**。hotfixブランチを remote に出すのも /ship が行う（CCはローカルcommitのみ）。

## 自走ポリシー（聞かない・自決して記録）
- **検証環境**: ローカルスタック（Docker/`supabase start`）を自分で起動。落ちていたら起動（§0）。
- **技術選定/スコープ**: 既存パターン準拠＋「決定と理由」1行記録。
- **要件の曖昧さ**: 設計メモ・既存コードから意図が一意なら §2(b) 確定。一意に決まらない**業務判断だけ**要回答（A/B/C＋推奨）。

## 冪等・安全
- 各フェーズは前段完了分だけ前進。1ループで同一 page_id を二度処理しない。自 page_id 以外は対象外。
- ゲート/終端（要回答・レビュー待ち・本番待ち・保留・完了）は触らない・ディスパッチャも dedup で再通知しない。
- 破壊的操作（ファイル削除等）は実行前に確認。**自分の生成物でない残骸（macOS " 2" 複製等）は触らない**。本番DB削除は人間ゲート。
- 課金: プラン枠で落ちる前提。勝手に API 課金へ流れる動作はしない。

## DRY_RUN 動作確認手順（副作用ゼロ）
1. 未整理を数件置く（材料十分な1件・本文空/曖昧な1件）。要件定義済みも数件（**🟢低を1件・🔴高を1件・🧱土台を1件・緊急を1件**）。
2. `/run --dry-run` → 提示のみ：盤面／「(b)→要件化＋4プロパティ付与・(a)→要回答」／**緊急プリエンプト予定**／要件定義済み各件の**レーン振り分け**（🟢低→自動マージ→本番待ち予定／🔴高・🧱土台→**Gemini二重レビュー予定**→レビュー待ち予定）／実行予定の**自動ゲート一覧**／付与予定 リスク/土台／**ディスパッチャ通知予定**（🧱土台即時／レビューバッチ／📦エピックship／要回答）。Notion/コード/通知/テスト/マージは一切しない。
3. `node scripts/dispatcher.mjs --dry-run` → 現盤面から各トリガーの発火予定だけ列挙（送信せず・state更新なし）。エピックが全て本番待ちなら「📦エピックship可 N件(🔴/🧱/🟢)」の発火予定が出る。
4. 実行（flagなし）で：🟢低が**本番待ち**へ・🔴高/🧱土台が**レビュー待ち**へ積まれ、🧱土台に**即時通知**・🟢leaf は**バッチ**に溜まること、エピック全完了で**📦エピックship通知**が出ること、再 `/run` で**再通知されない**（dedup）ことを確認。

## 通知（既存 LINE human-in-the-loop に1通）
この実行でそのゲートに“入れた”時だけ1通（既存パーク分は再通知しない）。本番ゲート（push/ship待ち）も通知。PRマージ承認（`ship承認`）とは別 kind・別文面で混線させない。
```bash
node scripts/notify-humanball.mjs --kind 要回答      --task "<タスク名>" --detail "<A/B/C＋推奨を1〜2行>" [--url "<session>"]
node scripts/notify-humanball.mjs --kind レビュー待ち --task "<タスク名>" --detail "実装+devマージ済み。<リスク/土台・二重レビューverdict>。🧪人力チェック手順は本文: <Notion page url>" [--url "<session>"]
# ディスパッチャは scripts/dispatcher.mjs が上記経由で配信（kind: 🧱土台/レビュー可/shipクラスタ/判断待ち/drain停止）
```

## 厳守
- 本番反映（push / deploy / db push / ship）と **レビュー待ち→本番待ち昇格** は人間の明示OKまで実行しない。それ以外は自走。
- **停止は §6 の3つ（要件定義済み枯渇／本番ゲート／異常）だけ**。**レビュー待ち・本番待ち・マージ待ち（dev堆積）が何件溜まっても止めない**（ディスパッチャ閾値は通知であって停止条件ではない）。「堆積したから区切る」は禁止。
- **緊急（`優先順位=緊急`）は main派生（origin/{{PROD_BRANCH}}基点）hotfix ブランチで実装**し、ゲート緑→**本番待ち（レビュー待ちを経由しない）＋🚨緊急通知**。**push/PR/Merge はしない**（人＝/ship）。通常チケットは従来どおり dev基点。
- **低リスクは dev自動マージ→本番待ち（レビュー不要）／中高土台は レビュー待ち止まり**。リスクのレーン分岐を必ず守る。
- **中/高/土台 をレビュー待ち/本番待ちに置くときは 🧪人力チェック手順 を本文に必ず生成**（§人力チェック手順・🔴🧱=詳細/🟡=要点）。通知は要約＋本文リンク・全文は本文。🟢低リスクは不要。
- 人ボール化（要回答）は人の回答まで再着手しない（同じ問いでループしない）。
- リスク/土台/エピック/優先度は必ず **Select/Checkbox プロパティ値**に入れる（本文・独自タグで代替しない）。
- 「🤖AI実行中」タグは着手で付け、本番待ち/レビュー待ち/要回答/blocked/中断で必ず外す。原則同時1件。
- `--dry-run` は副作用ゼロ。いつでも中断可能。
