{
  "buildCommand": "nuxt generate",
  "outputDirectory": ".output/public",
  "devCommand": "nuxt dev",
  "installCommand": "npm install --no-package-lock",
  "framework": "nuxtjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
