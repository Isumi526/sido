-- ============================================================
--  supabase/migrations/20260727000000
--  作業員マスタに読み仮名(name_kana)を追加
--   - 作業員一覧/選択リストを50音順で並べるための読み仮名カラム
--   - 手入力（自動生成しない）・nullable。未設定はソート時に name へフォールバック
--   - 既存の sites.name_kana（20260608000000）と同じ設計
--  追加のみDDL（後方互換・既存データに影響なし）
-- ============================================================

alter table workers add column if not exists name_kana text;
