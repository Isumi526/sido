-- ============================================================
--  日報に「出張」区分を追加（出張手当 +¥3,000/日 の対象フラグ）
--  追加のみ（ADD COLUMN・既定 false で後方互換）。
--  手当の現場帰属は集計側で「その日最も稼働時間が長い現場に1回」計上し二重計上を防ぐ。
-- ============================================================
alter table daily_reports add column if not exists is_business_trip boolean not null default false;
