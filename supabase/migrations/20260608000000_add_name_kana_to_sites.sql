-- ============================================================
--  supabase/migrations/20260608000000
--  現場マスタに読み仮名(name_kana)を追加
--   - 現場選択リストを50音順で並べるための読み仮名カラム
--   - 手入力（自動生成しない）・nullable。未設定はソート時に末尾フォールバック
-- ============================================================

alter table sites add column if not exists name_kana text;
