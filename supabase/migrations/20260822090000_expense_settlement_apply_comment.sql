-- ============================================================
--  経費 月次精算: 申請時の一言コメント（お詫び・依頼文）を保存する列を追加
--  正典: docs/spec/expense.md §4 / チケット c9bf71a4
--  - 作業員が経費を申請（承認依頼を送る）時に一言コメントを必須入力させ、
--    その内容を精算行に載せる。管理画面のレビュー詳細と経理宛メールで表示する。
--  - 追加のみ・後方互換。既存テーブル・データ・grant に変更なし。
--    expense_settlements は RLS 無効＝テーブル単位 grant のため新列は自動で読める。
-- ============================================================

alter table expense_settlements
  add column if not exists apply_comment text;  -- 申請時の一言コメント（お詫び・依頼文）
