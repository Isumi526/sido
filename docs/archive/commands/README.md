# _archive — 退役したスラッシュコマンド

このディレクトリのファイルは **`.claude/commands/` 直下から外した＝コマンドとして無効化**したもの。
履歴・再設計の参照用に残す（削除はしない）。現行の自走パイプラインは **`/run`（自走ループ・リスク2レーン）/ `/review`（人力レビュー→本番待ち昇格）/ `/ship`（本番反映ゲート）** の3本。

退役日: 2026-06-29

---

## spec-to-impl.md（退役）
- **理由**: 機能が **`/run` に完全包含**（要件定義書→詳細設計→実装→レビュー待ち の流れは /run の groom→実装→レーン着地で代替）。
- **非互換**: 手順4「★一旦停止し詳細設計書の承認を待つ（30秒レビュー）」が **/run の no-confirmation ポリシー**（『続けていいか』を聞かず枯渇まで自走）と正面衝突。
- **鮮度**: 2026-06-04 の新規追加コミット以降 **更新ゼロ**＝/run 誕生(06-08)・リスク2レーン(06-18)・ドレイン徹底(06-28) いずれも未反映。
- **固有価値**: 残存なし。再利用予定なし。

## next.md（暫定退役）
- **理由（安全ギャップ）**: 旧レーンロジックで **全リスクを一律「本番待ち」直行**（本文「レビュー待ちは経由しない＝自己チェックで本番待ちへ」）。現行2レーンでは **中/高/土台＝レビュー待ち止まり**が必須。
  → /next で 🔴高（認証/RLS/課金）を処理すると **レビュー待ち＋Gemini二重レビュー＋RLS監査＋🧪人力チェック手順を全部スキップして本番待ち**に到達し、唯一の人間ゲート（レビュー待ち→本番待ち昇格）を迂回する回帰になる。
- **未反映**: `リスク/土台/エピック`プロパティ付与・`🤖AI実行中`タグ・ディスパッチャ・棚卸し(groom)・緊急main派生レーン（すべて 06-18/06-28 導入）。
- **鮮度**: 2026-06-08 の軽微改訂(/run新設時)以降そのまま。run.md 更新履歴(06-18)に「handoff（ship.md / next.md）は不変」＝意図的に旧モデルのまま据え置かれていた。
- **役割は残る**: 「手動1件モード」は /run（単件モード無し）に対する隙間を埋める。**再利用する場合は『/run の2レーン版・単件』として要再設計**（🟢低のみ本番待ち直行／中高土台はレビュー待ち／risk・土台・エピック付与／🔴高はGemini＋RLS監査）。別チケットで復活させること。

---

## 復活のしかた
`.claude/commands/_archive/<file>` を `.claude/commands/<file>` に戻せばコマンドとして再登録される。
ただし next.md は上記の安全ギャップを解消（2レーン整合）してから戻すこと。
